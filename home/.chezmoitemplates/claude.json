{
  "hasCompletedOnboarding": true,
  "theme": "dark",
  "deepLinkTerminal": "Ghostty",
  "showExpandedTodos": true,
  "showSpinnerTree": true,
  "hasSeenTasksHint": true,
  "hasOpenedAgentsView": true,
  "officialMarketplaceAutoInstallAttempted": false,
  "officialMarketplaceAutoInstalled": false,
  "mcpServers": {
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@upstash/context7-mcp@{{ .mcp.context7 }}"
      ]
    },
    "chrome-devtools": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@{{ .mcp.chromeDevtools }}"
      ]
    },
    "antigravity": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@ask-llm/antigravity-mcp@{{ .mcp.antigravity }}"
      ]
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@{{ .mcp.playwright }}"
      ]
    },
{{- /* dart / xcode MCP は macOS 専用バイナリ (dart, xcrun) に依存する */}}
{{- if eq .chezmoi.os "darwin" }}
    "dart": {
      "type": "stdio",
      "command": "dart",
      "args": [
        "mcp-server"
      ]
    },
    "xcode": {
      "type": "stdio",
      "command": "xcrun",
      "args": [
        "mcpbridge"
      ]
    },
{{- end }}
    "figma": {
      "type": "http",
      "url": "https://mcp.figma.com/mcp"
    },
    "drawio": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@drawio/mcp@{{ .mcp.drawio }}"
      ]
    },
{{- if and (ne .chezmoi.os "windows") (not (dig "skip_cli_tools" true .)) }}
{{- /* gemini-cli-extensions/nanobanana 同梱の MCP server。.chezmoiexternal.toml が source を展開し、
      .chezmoiscripts/run_onchange_after_95_nanobanana-mcp.sh.tmpl が build した dist を起動する。
      gate は build に必要な node の導入条件 (mise config の node と同じ式) に揃える。
      GEMINI_API_KEY は server 側で NANOBANANA_API_KEY 不在時の fallback として読まれる */}}
    "nano-banana": {
      "type": "stdio",
      "command": "node",
      "args": [
        "{{ .chezmoi.homeDir }}/.local/share/nanobanana-mcp/mcp-server/dist/index.js"
      ],
      "env": {
        "GEMINI_API_KEY": "{{ dig "apiKeys" "gemini" "" . }}"
      }
    },
{{- end }}
    "maps-grounding-lite": {
      "type": "http",
      "url": "https://mapstools.googleapis.com/mcp",
      "headers": {
        "X-Goog-Api-Key": "{{ dig "apiKeys" "googleMaps" "" . }}"
      }
    }
  }
}
