{{ if and (eq .chezmoi.os "darwin") (not (dig "skip_cli_tools" false .)) -}}
#!/bin/bash
set -euo pipefail

{{ include "../install/lib.sh" }}
activate_tool_paths

# mole (mo) の最適化は対話を伴いうるため CI ではスキップする
if [ -z "${CI:-}" ]; then
    mo optimize
    mo clean
fi
{{ end -}}
