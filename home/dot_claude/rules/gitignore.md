---
paths:
  - "**/.gitignore"
---
# gitignore

- Whitelist Format: When creating a new `.gitignore`, ignore everything by default (`*`) and explicitly allow tracked paths with `!`. When editing an existing `.gitignore`, keep its current style and add entries in that style; convert a blacklist-style file to whitelist only when asked.
